
    <div class="sidebar" data-color="purple" data-image="">
        {#<div class="sidebar" data-color="purple" data-image="{{ asset('assets/img/google-maps-new-interface.jpg') }}">#}

        <!--
            Tip 1: You can change the color of the sidebar using: data-color="purple | blue | green | orange | red"

            Tip 2: you can also add an image using data-image tag
        -->

        <div class="logo">
            <a  class="simple-text">
                Administración

            </a>

        </div>

        <div class="sidebar-wrapper">
            <ul class="nav">
                <li >
                    <a class="color">
                        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                            <i class="material-icons color">face</i>

                            {{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }}



                            {% else %}
                                <a href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
                            {% endif %}

                    </a>
                </li>
                <li >
                    <a href="{{ path('fos_user_profile_edit')}}">
                        <i class="material-icons">perm_identity
                        </i>

                        <p class="control-label">Editar perfil.</p>
                    </a>
                </li>
                <li >
                    <a href="{{ path('fos_user_change_password')}}">
                        <i class="material-icons">https</i>

                        <p class="control-label">Editar contraseña.</p>
                    </a>
                </li>
                {% if app.user.username=="poloni yamil" %}
                    <li >
                        <a href="{{ path('inicio')}}">
                            <i class="material-icons">settings</i>

                            <p class="control-label">Admin.. de usuarios.</p>
                        </a>
                    </li>
                {% endif %}

                <hr class="black">
                {#<li class="">#}
                    {#<a href="{{ path('admin')}}">#}
                        {#<i class="material-icons">dashboard</i>#}
                        {#<p>Home</p>#}
                    {#</a>#}
                {#</li>#}
                <li >
                    <a href="{{ path('admin_problematica_index')}}">
                        <i class="material-icons">bubble_chart</i>

                        <p class="control-label">Problematicas.</p>
                    </a>
                </li>
                <li class="divider"></li>

                <li>
                    <a href="{{ path ('admin_problematica_new') }}">
                        <i class="material-icons">create</i>
                        <p class="control-label">Alta de Problematicas.</p>
                    </a>
                </li>
                <li class="divider"></li>

                <li>
                    <a href="{{ path ('admin_problematica_mapa') }}">
                        <i class="material-icons">satellite</i>
                        <p class="control-label">Mapa.</p>

                    </a>
                </li>
                <hr>
                <li>
                    <a class="text-danger" href="{{ path('fos_user_security_logout') }}">
                        {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                        <i class="material-icons text-danger">reply
                        </i>

                    </a>
                </li>
                <hr>

            </ul>
        </div>
    </div>

    {% block javascripts %}
        <script src="{{ asset('assets/js/jquery-3.2.0.min.js') }}" ></script>
        <script src="{{ asset('assets/js/bootstrap.min.js') }}" ></script>
        <script src="{{ asset('assets/js/material.min.js') }}" ></script>

        <!--  Plugin for the Sliders, full documentation here: http://refreshless.com/nouislider/ -->

        <!--  Plugin for the Datepicker, full documentation here: http://www.eyecon.ro/bootstrap-datepicker/ -->

        <!-- Control Center for Material Kit: activating the ripples, parallax effects, scripts from the example pages etc -->
        <script src="{{ asset('assets/js/material-dashboard.js') }}"></script>








    {% endblock %}